---
EC2_INI_PATH: "{{lagoon_configuration_params.component_path[lagoon_provider]}}/inventory/config/ec2{{lagoon_network}}.ini"
AWS_REGION: "{{ lookup('env', 'AWS_REGION') | default('eu-west-1', true) }}"
